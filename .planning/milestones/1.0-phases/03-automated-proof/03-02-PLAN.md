---
phase: 03-automated-proof
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - internal/platform/gateproof/proof02_test.go
  - internal/platform/gateproof/ordering_test.go
autonomous: true
requirements: [PROOF-02, GATE-10]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`make gate`, spawned from inside the suite it governs with `EPISTEMIC_OS_DB_URL` set to a closed-port DSN, exits non-zero and prints the escalation preamble and the unreachable host `127.0.0.1:1` on ONE line of `RunResult.Combined` (PROOF-02, GATE-10, D-01, D-17, D-18)"
    - "The host token PROOF-02 asserts on is the one the test itself supplied, so the assertion couples to nothing outside the test — not to `testenv`'s prose, not to pgx's dial diagnosis, and not to golang-migrate's wrapper text (D-15, D-17)"
    - "The same nested gate run additionally shows `testenv` speaking on the unreachable-database path, which is D-03's ordering claim checked by its effect rather than by its structure, at zero extra cost because the run already happened (D-02b)"
    - "`make -n gate` expands to a recipe in which `env-preflight` precedes `migrate`, asserted structurally against the dry-run expansion rather than against the Makefile's raw text, so D-03's ordering claim rests on two independent checks each with a single reason to fail (D-02a)"
    - "The ordering check survives a trim of the expensive proofs: it does not depend on any nested `make gate` run, so if budget pressure ever removes a proof the ordering claim does not disappear with it silently (D-02)"
    - "PROOF-02's SC-5 control runs the same invocation against a copy of `HEAD` with the escalation export stripped and observes NO escalation preamble there, while the intact side produced one — and it does NOT assert golang-migrate's own reporter text, because that is a dependency's prose and pinning to it is Phase 2's carried warning 2 done on purpose (D-11, D-13, D-14, D-15)"
    - "The control reuses PROOF-02's captured intact-tree `RunResult` and adds only the defeated-tree run; `requireIntact` asserts that result is real before comparing, and running the control subtest alone makes that guard fail (D-16)"
    - "PROOF-02 (empty): the host token is non-empty by construction because the test supplies the DSN. If `hostFromURL` ever returns the empty string, `testenv` substitutes a fixed placeholder naming `EPISTEMIC_OS_DB_URL` instead of printing an empty host, so the same-line predicate is never asked to match the empty string; and if the nested gate exits 0 the vacuity guard fires before any content assertion runs"
    - "PROOF-02 (encoding): the host is compared as a raw ASCII byte substring `127.0.0.1:1` against one line of `Combined` — never a resolved, normalized or punycoded form, and never the loopback hostname, because this host has no IPv6 loopback listener and that hostname resolves there first"
  artifacts:
    - path: "internal/platform/gateproof/proof02_test.go"
      provides: "PROOF-02 and its SC-5 differential control as two subtests of one top-level test, plus D-02b's preamble-presence observation"
      contains: "func TestPROOF02_GateNamesUnreachableHost"
    - path: "internal/platform/gateproof/ordering_test.go"
      provides: "D-02a's structural ordering check over `make -n gate`'s dry-run expansion"
      contains: "func TestGateOrdersPreflightBeforeMigrate"
  key_links:
    - from: "internal/platform/gateproof/proof02_test.go"
      to: "internal/platform/gateproof/gateproof_test.go"
      via: "`unreachableDSN`, `lineContainsBoth` and `requireIntact` from the 03-01 spine"
      pattern: "lineContainsBoth|requireIntact|unreachableDSN"
    - from: "internal/platform/gateproof/ordering_test.go"
      to: "the `gate` target's recipe in the Makefile"
      via: "`make -n gate`'s dry-run expansion, read as subprocess output rather than as file text"
      pattern: "env-preflight"
    - "D-03's ordering claim now rests on two independent checks — the dry-run expansion and PROOF-02's observed reporter identity. That is the point: each has a single reason to fail, so a reword of one does not silently take both. If either is ever deleted, the claim quietly drops to one leg and nothing at the deletion site says so."
    - "PROOF-02's control is the one place in this phase where an assertion was deliberately NOT added. If a later edit adds a reporter-identity assertion for the defeated side here, it will be binding a proof to golang-migrate's prose, and a dependency bump will break Phase 3 for a reason nobody chose."
  prohibitions:
    - "No assertion added by this plan may bind to text this project does not own. The defeated side of PROOF-02's control gets no reporter-identity assertion, because the reporter there is golang-migrate. Naming what a dependency says is a coupling whose breakage reads as a proof defect rather than as the dependency change it is."
    - "No assertion in this plan may be structurally incapable of coming back false. The ordering check must fail if `migrate` is moved before `env-preflight`; PROOF-02's same-line predicate must fail if the preamble and the host land on separate lines; and the shared-result guard must fail when the proof did not run."
    - "No proof may buy its green by weakening the mechanism it proves. Nothing here may edit the working tree's Makefile, set or override the escalation flag for the outer suite, or reach for a narrower Make target than `gate` — the narrower target was measured to exit 0 against a closed port, so it proves a weaker claim, not the same claim more cheaply."
---

<objective>
Expand the proven slice horizontally to the second condition: prove the gate fails
and names the host it could not reach, prove its SC-5 differential control, and add
the two cheap structural checks that carry D-03's ordering claim independently of the
expensive proofs.

Purpose: PROOF-02 adds no new layer. 03-01 established the package, the harness's
`Dir` field, the tree materialiser, the same-line predicate and the proof/control
subtest shape; this plan applies all five to the unreachable-database condition and
adds the ordering checks that must survive a future trim of the proofs. After it, two
of the three conditions are proven and controlled.

Output: `TestPROOF02_GateNamesUnreachableHost` with both subtests, and
`TestGateOrdersPreflightBeforeMigrate`.

## Cost, from CONTEXT.md's measurements — not re-estimated

PROOF-02's intact run was measured at **6.4s warm, exit 2** and **9s cold after a
warm parent** (D-05's tables). Its control adds one defeated-tree run. The ordering
check is a `make -n` dry run and costs a fraction of a second — which is the whole
argument for it existing separately (D-02): it survives any budget pressure that
trims a nested gate, and the loss of an expensive proof is then visible rather than
silently taking the ordering claim with it.

## Why the ordering claim gets its own check rather than riding on the proofs (D-02)

Two reasons, both given by the operator during discussion, and stated here because
this is the plan that implements them.

**Each check gets a single reason to fail.** This is the generalised form of Phase
2's carried warning 2, and it is why a claim resting on two independent checks
survives a reword of either. The dry-run expansion is a *structural* claim checked
*structurally* — D-07's non-uniform-weakness objection was about *output*, so it does
not transfer to ordering, and the text-check objection that killed a Makefile-text
assertion in 02-02 does not apply to a `make -n` expansion, which is the real
recipe as Make resolved it.

**It degrades gracefully.** Three nested gates will create pressure to trim. If the
ordering claim were only a side effect of the expensive proofs, it would disappear
with them silently. Here the cheap assertion survives the trim and the loss is
visible.

This reasoning was stated as holding independent of what the measurement returned,
and the measurement did not change it.
</objective>

<execution_context>
@C:/EpistemicOS/epistemicos-gsd-pilot/.claude/gsd-core/workflows/execute-plan.md
@C:/EpistemicOS/epistemicos-gsd-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-automated-proof/03-CONTEXT.md
@.planning/phases/03-automated-proof/03-PATTERNS.md
@.planning/phases/03-automated-proof/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PROOF-02 — the gate names the host it could not reach</name>

  <files>internal/platform/gateproof/proof02_test.go</files>

  <read_first>
    - `internal/platform/gateproof/gateproof_test.go` (the 03-01 spine — `unreachableDSN`, `lineContainsBoth`, `requireIntact`, and the package doc comment stating the exported-surface-only boundary)
    - `internal/platform/gateproof/proof01_test.go` (the proof/control subtest shape this task mirrors exactly: `gate.SkipIfNested(t)` first, a `gate.RunResult` in the parent scope, `intact_tree` then `defeated_tree_control`)
    - `internal/platform/testenv/testenv.go` lines 155-190 (`Pool`'s unreachable-database branch and its escalated `t.Fatalf` at line 182, which formats preamble, host, `URLEnv` and the pgx error into ONE call — this is PROOF-02's target and GATE-10's subject; also `hostFromURL` at 112 and the empty-host placeholder just above the Fatalf)
    - `internal/platform/gate/makefile_test.go` lines 8-17 (`closedPortDSN` and its comment explaining that a closed local port reproduces the unreachable-database condition without stopping the compose service — 03-01 redeclared its equivalent as `unreachableDSN` because D-03 forbids reaching the unexported original)
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` (D-01's measured table showing PROOF-02's shape at 6.4s exit 2; D-02b; D-15's refusal to assert golang-migrate's text; D-17; D-18)
  </read_first>

  <behavior>
    - `TestPROOF02_GateNamesUnreachableHost/intact_tree` with `EPISTEMIC_OS_DB_URL` set to `unreachableDSN`: the nested `make gate` exits non-zero, and some single line of `RunResult.Combined` contains both `testenv.RequireEnv` and the literal host token `127.0.0.1:1`.
    - The same captured result shows the escalation preamble present at all — D-02b's effect check on D-03's ordering claim, obtained from a run that already happened.
    - The test skips without running its body when the depth marker is 1 or more.
    - The compose PostgreSQL service is still running and reachable after the test — nothing was stopped to reproduce the condition.
  </behavior>

  <action>
Write `internal/platform/gateproof/proof02_test.go` with
`TestPROOF02_GateNamesUnreachableHost`, mirroring `proof01_test.go`'s structure
exactly so a reader comparing the three conditions sees one shape three times rather
than three shapes.

The doc comment carries the hyphenated requirement ID for the unreachable-host proof,
states the requirement — the gate fails and names the unreachable host — and records
D-01 as `proof01_test.go` does, with PROOF-02's own measured row: `make gate` against
a closed-port DSN exited 2 in 6.4s warm, while `make env-preflight` against the same
DSN exited **0** in 5.6s, because the escalation export is target-scoped to `gate`.
The cheaper target did not prove the same claim more cheaply; it proved a weaker one.

`gate.SkipIfNested(t)` is the function's literal first statement. Declare a
`gate.RunResult` in the function scope for the intact result, then
`t.Run("intact_tree", ...)`:

- Invoke `gate.Make` with target `gate` and a `gate.RunOptions` whose `Env` carries
  one entry setting `testenv.URLEnv` to `unreachableDSN`. Use `Env`, not `Unset` —
  the condition PROOF-02 names is a *set but unreachable* database, and unsetting
  would reproduce PROOF-01's condition instead. Assign the result to the outer
  variable.
- Vacuity guard first, before any content assertion, in the `makefile_test.go:43-47`
  shape: fail on `TimedOut` naming the timeout, and fail on `ExitCode == 0` stating
  that `make gate` exited 0 against a closed port so the unreachable-database
  condition was not reproduced and every assertion below would be vacuous.
- The single content assertion: `lineContainsBoth` over `Combined`,
  `testenv.RequireEnv` and the literal host token `127.0.0.1:1`. The comment records
  D-17: the host is the value the test itself supplied, so the assertion couples to
  nothing — not to `testenv`'s prose, which `testenv` could reword, and not to pgx's
  dial diagnosis, which pgx could reword. It also records D-18: whole-blob
  `strings.Contains` would pass on a run that skipped, because the same tokens appear
  in `testenv`'s lenient skip message, and a non-zero exit adds nothing as a conjunct
  because exit 2 was measured in all six cells.
- Immediately after, and in the same subtest, add D-02b's observation: assert that
  `testenv.RequireEnv` appears in `Combined` at all. Its comment states what it is
  and why it is here rather than in its own test — it is the *effect* half of D-03's
  ordering claim (`env-preflight` runs before `migrate`, so `testenv` and not
  `store.RunMigrations` is the first voice on this path), obtained from a nested gate
  that was going to run anyway, at zero extra cost. The *structural* half is
  `TestGateOrdersPreflightBeforeMigrate` in Task 3, and the two are deliberately
  independent.

Reference only the harness's exported surface, and only `testenv`'s exported
constants. Do not add Go's parallel-test marker.
  </action>

  <verify>
    <automated>go vet ./internal/platform/gateproof/ &amp;&amp; go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost' -count=1 -v</automated>
    <automated>docker compose ps postgres</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost' -count=1 -v` exits 0 with at least two `--- PASS` lines and **zero** `--- SKIP` lines.
    - `EPISTEMIC_OS_TEST_MAKE_DEPTH=1 go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost' -count=1 -v` shows the test skipped and exits 0.
    - `grep -c '127.0.0.1:1' internal/platform/gateproof/proof02_test.go` is at least 1 — the assertion binds to the test-supplied host token.
    - `! grep -nE 'localhost' internal/platform/gateproof/proof02_test.go` — `127.0.0.1` is used throughout; this host has no IPv6 loopback listener and the hostname resolves there first.
    - `grep -c 'testenv.RequireEnv' internal/platform/gateproof/proof02_test.go` is at least 2 — the same-line assertion and D-02b's presence observation.
    - `! grep -nE '(escalationPreamble|closedPortDSN|extraLines)' internal/platform/gateproof/proof02_test.go` — the exported-surface boundary holds.
    - `docker compose ps postgres` shows the service still running after the test — the condition was reproduced without stopping anything.
    - `git status --porcelain` is empty.
  </acceptance_criteria>

  <done>
PROOF-02 proves the gate fails and names `127.0.0.1:1` on one line when the database
is set but unreachable, binding only to a value the test supplied and an exported
constant this project owns. The same run carries D-02b's effect check on D-03's
ordering claim, for free. The compose service was never stopped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROOF-02's SC-5 differential control, with the assertion D-15 forbids left out</name>

  <files>internal/platform/gateproof/proof02_test.go</files>

  <read_first>
    - `internal/platform/gateproof/proof02_test.go` (Task 1 — the parent test and its captured intact result)
    - `internal/platform/gateproof/proof01_test.go` (03-01 Task 3 — the control subtest shape, the D-16 guard call site and its comment, and the D-11 entailment comment this task repeats for its own condition)
    - `internal/platform/gateproof/tree_test.go` (03-01 — `materializeTree` and `stripEscalationExport`)
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` (**D-14's six-cell table** — PROOF-02's defeated row shows exit 2 with the preamble ABSENT and golang-migrate as the reporter; **D-15** in full, which is the decision this task implements by omission)
    - `internal/adapters/secondary/store` migration wiring — enough to confirm that in the defeated tree the failing reporter is golang-migrate's wrapper and not this project's own text, which is the measured basis for D-15's refusal
  </read_first>

  <behavior>
    - With the escalation export stripped from the copy's `Makefile` and the same closed-port DSN supplied, `make gate` run in that copy exits non-zero and its `Combined` contains NO occurrence of `testenv.RequireEnv`.
    - Running only `TestPROOF02_GateNamesUnreachableHost/defeated_tree_control` makes the shared-result guard fail: the captured intact result is the zero value.
    - The intact side is not re-run — exactly one intact `make gate` invocation exists in this file.
    - The control asserts nothing about which dependency reported the failure in the defeated tree.
  </behavior>

  <action>
Add `t.Run("defeated_tree_control", ...)` to `TestPROOF02_GateNamesUnreachableHost`,
reading the intact `gate.RunResult` the `intact_tree` subtest captured.

Open with `requireIntact` on that captured result. Repeat, at this call site, the
comment D-16 requires — it is not checking that the proof passed, it is checking that
the proof RAN — because the `if` will read as redundant here exactly as it does in
`proof01_test.go`, and a reader who meets only this file must find the reason here.

Build the defeated side identically to PROOF-01's control: `materializeTree`, then
`stripEscalationExport` on its root, then `gate.Run` with argv `["make", "gate"]` and
a `gate.RunOptions` whose `Dir` is that root and whose `Env` carries the same
`testenv.URLEnv` to `unreachableDSN` entry as the intact side. The ONLY difference
between the two sides is the stripped export.

Two assertions, and deliberately not a third:

1. `lineContainsBoth` over the captured intact `Combined`, `testenv.RequireEnv` and
   `127.0.0.1:1` must be true — the intact side's positive, restated at the
   comparison so both sides appear together.
2. `strings.Contains` of the defeated `Combined` for `testenv.RequireEnv` must be
   **false**. The comment states D-11's entailment in full: the proof asserts the
   gate fails and names its cause; if the defeated tree lacked the escalation
   preamble the proof binds to, the proof would fail — proving the premise proves the
   consequent. It also records D-14's measured cell for this condition: with the
   export stripped, exit is still 2 and the preamble is ABSENT, because `migrate`
   becomes the reporter and fails anyway. That is D-03's ordering point measured from
   the other side, and it is why the preamble and not the exit code is the
   discriminator.

**The third assertion is deliberately absent, and the code must say so.** Write a
comment where a reporter-identity assertion would go, recording D-15: PROOF-01's
control asserts `config`'s message because `config` is ours; PROOF-02's control does
**not** assert golang-migrate's `new migrator` wrapper text, because that is a
dependency's prose and pinning to it is Phase 2's carried warning 2 done on purpose.
The "who spoke" signal is obtained here without reintroducing a defect already paid
for. A comment is used rather than silence because an absent assertion is invisible,
and the next reader adding one for symmetry with PROOF-01's control would be
reintroducing exactly what was removed.
<!-- planner-discipline-allow: new migrator -->

Do not assert on the defeated side's exit code as a conjunct.
  </action>

  <verify>
    <automated>go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost' -count=1 -v</automated>
    <automated>go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost/defeated_tree_control' -count=1; test $? -ne 0 &amp;&amp; echo GUARD-DEMONSTRATED-FAILING</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost' -count=1 -v` exits 0 with three `--- PASS` lines and zero `--- SKIP` lines.
    - `go test ./internal/platform/gateproof/ -run 'TestPROOF02_GateNamesUnreachableHost/defeated_tree_control' -count=1` exits NON-ZERO — the D-16 guard is demonstrated able to fail.
    - `grep -c 'gate.Make(' internal/platform/gateproof/proof02_test.go` is exactly `1` — the intact side runs once.
    - `! grep -n 'new migrator' internal/platform/gateproof/proof02_test.go` **as an assertion argument**: the phrase may appear only inside the D-15 comment explaining why it is not asserted, and `grep -n 'strings.Contains' internal/platform/gateproof/proof02_test.go` must show no call whose argument is that phrase.
    - `! grep -nE 'defeated\.ExitCode' internal/platform/gateproof/proof02_test.go`.
    - `grep -c 'D-15' internal/platform/gateproof/proof02_test.go` is at least 1 — the absent assertion is documented rather than invisible.
    - `git status --porcelain` is empty, and `git diff --stat -- Makefile` prints nothing.
    - `go test ./internal/platform/gateproof/ -count=1 -shuffle=on` passes.
  </acceptance_criteria>

  <done>
PROOF-02's control shows the escalation preamble present in the intact tree and
absent in a copy with the export stripped, without binding to any text this project
does not own. The place where a reporter-identity assertion would have gone carries
the reason it is not there, so its absence is a decision on the record rather than an
omission a later reader corrects.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The structural half of D-03's ordering claim — `make -n gate` expands preflight before migrate</name>

  <files>internal/platform/gateproof/ordering_test.go</files>

  <read_first>
    - `Makefile` lines 63-90 (`env-preflight`, the `gate` target's recipe — `vet`, the gofmt loop, `go build ./...`, then `$(MAKE) env-preflight`, `$(MAKE) migrate`, `$(MAKE) test` — and `migrate`)
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` (**D-02** in full — both reasons, and the statement that the reasoning holds independent of what the measurement returned)
    - `.planning/phases/02-enforcement-and-a-single-gate-definition/02-CONTEXT.md` D-03 and D-07 (the voiceless-preflight ordering claim this check carries, and the non-uniform-weakness objection that rejected a Makefile-*text* assertion for the *output* claim — read it to see why it does not transfer to an ordering claim checked against a dry-run expansion)
    - `internal/platform/gate/harness.go` lines 259-270 (`Make`, and its `Silent` handling — this check needs the recipe expansion, so consider whether the silent flag suppresses what it must read)
    - `internal/platform/gateproof/gateproof_test.go` (the spine — `gate.SkipIfNested(t)` first, no parallel marker)
  </read_first>

  <behavior>
    - `make -n gate` exits 0 and its combined output contains a line invoking `env-preflight` and a later line invoking `migrate`.
    - The index of the first `env-preflight` invocation line is strictly less than the index of the first `migrate` invocation line.
    - If the two recipe lines were transposed in the Makefile, this check fails — the assertion is a strict ordering comparison, not a presence check for both tokens.
    - The check performs no nested `make gate` run and completes in well under a second, so it survives any future trim of the expensive proofs.
  </behavior>

  <action>
Write `internal/platform/gateproof/ordering_test.go` with
`TestGateOrdersPreflightBeforeMigrate`.

`gate.SkipIfNested(t)` is the function's literal first statement — the dry run spawns
a `make` subprocess through the harness like every other test here, so it inherits
the same contract.

Invoke the harness with argv `["make", "-n", "gate"]` via `gate.Run`, with a
zero-value `gate.RunOptions` apart from any timeout you choose to shorten. `-n` is
Make's dry run: it prints the recipe as Make resolved it, with variables expanded and
`$(MAKE)` recursion shown, without executing anything. Use `gate.Run` rather than
`gate.Make` because the flag must precede the target and `Make`'s argv builder places
only `-s` there.

Guard first: fail if the run timed out, and fail if `ExitCode` is non-zero, stating
that the dry-run expansion could not be obtained so the ordering assertion below
would be vacuous. A dry run of a well-formed Makefile exits 0 even when the real
recipe would fail, so a non-zero exit here means the expansion itself is broken.

Then split `Combined` on the newline byte, trimming a trailing carriage return per
line as `lineContainsBoth` does, and find the index of the first line invoking
`env-preflight` and the index of the first line invoking `migrate`. Fail naming the
whole expansion if either is absent. Then assert the preflight index is **strictly
less than** the migrate index, and fail with both indices and both lines named if it
is not.

The doc comment carries D-02a's reasoning and D-03's claim. State three things
explicitly, because each will look like an over-explanation later and each is
load-bearing:

- **What the claim is:** `env-preflight` runs before `migrate`, so `testenv` and not
  `store.RunMigrations` is the first reporter for an unreachable database. If the two
  are transposed, golang-migrate's unaudited wrapper text becomes the first voice and
  satisfies neither GATE-01 nor GATE-02, and nothing at the transposition site would
  say so.
- **Why this is checked structurally when 02-02 refused a Makefile-text check.** D-07
  rejected a text assertion for the *voicelessness* claim on a non-uniform-weakness
  argument: that constraint is about *output*, and the likely decay is a tool inside
  the step starting to print, which text cannot see. Ordering is a structural claim,
  and `make -n`'s expansion is the recipe as Make resolved it rather than the file's
  raw bytes — so the objection does not transfer.
- **Why it is separate from the proofs.** Each check gets a single reason to fail, so
  a claim resting on two independent checks survives a reword of either; and three
  nested gates will create pressure to trim, so an ordering claim that existed only
  as a side effect of an expensive proof would disappear with it silently. Here the
  cheap assertion survives the trim and the loss is visible. Name PROOF-02's
  in-run preamble observation (D-02b) as the other leg, so a reader deleting one
  knows what the claim drops to.

Do not add Go's parallel-test marker.
  </action>

  <verify>
    <automated>go vet ./internal/platform/gateproof/ &amp;&amp; go test ./internal/platform/gateproof/ -run 'TestGateOrdersPreflightBeforeMigrate' -count=1 -v</automated>
    <automated>make -n gate</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./internal/platform/gateproof/ -run 'TestGateOrdersPreflightBeforeMigrate' -count=1 -v` exits 0 with one `--- PASS` and zero `--- SKIP`, and completes in under 5 seconds.
    - The test performs no nested `make gate` invocation: `! grep -nE '"gate"\s*[,}]' internal/platform/gateproof/ordering_test.go` other than inside the `-n` argv, and `grep -c '"-n"' internal/platform/gateproof/ordering_test.go` is at least 1.
    - The assertion is a strict index comparison, not two presence checks: `grep -nE '<' internal/platform/gateproof/ordering_test.go` shows the comparison between the two resolved indices.
    - Falsifiability, demonstrated once at execution time and recorded in the SUMMARY rather than committed: in a `materializeTree` copy, transpose the `$(MAKE) env-preflight` and `$(MAKE) migrate` lines in the copy's Makefile and run this test with the harness pointed at that copy — it must FAIL. Record the observed failure message in the SUMMARY. Do not commit the transposition, and do not add a permanent test that depends on it.
    - `EPISTEMIC_OS_TEST_MAKE_DEPTH=1 go test ./internal/platform/gateproof/ -run 'TestGateOrdersPreflightBeforeMigrate' -count=1 -v` shows the test skipped and exits 0.
    - `git status --porcelain` is empty and `git diff --stat -- Makefile` prints nothing.
  </acceptance_criteria>

  <done>
D-03's ordering claim now has a structural check that reads the recipe as Make
resolved it, costs a fraction of a second, and is demonstrated to fail when the two
steps are transposed. Together with PROOF-02's in-run preamble observation the claim
rests on two independent legs, each with one reason to fail, and neither disappears
if the expensive proofs are ever trimmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → `make` subprocess | PROOF-02 spawns a full `make gate`; the ordering check spawns `make -n gate`, which executes nothing but still runs Make with the ambient environment |
| Test process → `git` subprocess | `materializeTree` spawns `git archive --format=tar HEAD` for the control's defeated copy |
| Constructed DSN → child environment | `unreachableDSN` is layered onto the child's `EPISTEMIC_OS_DB_URL`, and its dial failure text is captured and may be printed |
| Test output → CI log | Failure reports print the whole `RunResult.Combined` blob, including pgx's dial diagnosis for the closed-port DSN |

## STRIDE Threat Register

ASVS L1; `security_block_on: high`. Severity is impact x likelihood.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | nested `make gate` recursion | **high** | mitigate | Carried unchanged from 03-01 and re-applied here: `gate.SkipIfNested(t)` is the literal first statement of both new tests, `buildChildEnv`'s `DepthEnv`-last ordering is untouched, and `DefaultTimeout` bounds every child. The acceptance criteria run the package with the depth marker set to 1 and assert the tests skip — measured, not asserted |
| T-03-02 | Tampering | defeat edits reaching the working tree | **high** | mitigate | The control's defeat happens only inside a `materializeTree` copy via `stripEscalationExport`, which rejects a root equal to `RepoRoot(t)`. The ordering check's falsifiability demonstration is explicitly required to happen in a copy, to be recorded in the SUMMARY, and NOT to be committed. Acceptance criteria assert `git status --porcelain` and `git diff --stat -- Makefile` are both empty |
| T-03-03 | Information Disclosure | pgx dial diagnosis in failure output | medium | mitigate | PROOF-02's DSN is a throwaway by construction — user `u`, password `pw`, a closed local port, a database that does not exist — so the `user=` / `database=` disclosure pgx emits on a dial failure carries no real credential. This is Phase 1's AR-01 residual reaching a new output site, on the same terms 02-02 recorded at T-02-09 |
| T-03-04 | Tampering | binding an assertion to a dependency's prose | medium | mitigate | The bespoke risk this phase carries. PROOF-02's control asserts nothing about golang-migrate's reporter text, and the place where such an assertion would go carries the reason it is absent, so a later reader adding one for symmetry meets the decision first. Enforced by an acceptance criterion, not by convention |
| T-03-05 | Denial of Service | shared compose database contention | medium | accept | Unchanged from 03-01's T-03-06: measured at `gate` x `store` +2.11s and `gate` x `approved` +0.80s concurrent, present today before Phase 3, declared under D-06 and registered as a deferred item by 03-04 |
| T-03-06 | Spoofing | `make` and `git` resolved from `PATH` | low | accept | Same terms as Phase 1's AR-03 and Phase 2's T-02-11 |
| T-03-SC | Tampering | dependency installation | low | accept | No package-manager install task exists in this plan; both new files use only the standard library and this repository. `go.mod` and `go.sum` are untouched |
</threat_model>

## Artifacts this phase produces

This plan's contributions are marked **(here)**. The full phase surface is repeated
so an executor of this plan alone sees it.

**New package — `internal/platform/gateproof`** (test-only)

| Symbol | File | Kind | Plan |
|---|---|---|---|
| `unreachableDSN`, `fixturePathSuffix`, `lineContainsBoth`, `requireIntact` | `gateproof_test.go` | consts + funcs | 03-01 |
| `TestLineContainsBoth_Control`, `TestGateproof_NoParallelMarker` | `gateproof_test.go` | tests | 03-01 |
| `materializeTree`, `stripEscalationExport`, `TestMaterializeTree_Control` | `tree_test.go` | funcs + test | 03-01 |
| `TestPROOF01_GateNamesUnsetURL` (+ `intact_tree`, `defeated_tree_control`) | `proof01_test.go` | test | 03-01 |
| `TestPROOF02_GateNamesUnreachableHost` (+ `intact_tree`, `defeated_tree_control`) | `proof02_test.go` | test | 03-02 **(here)** |
| `TestGateOrdersPreflightBeforeMigrate` | `ordering_test.go` | test | 03-02 **(here)** |
| `breakFixture` | `proof03_test.go` | func | 03-03 |
| `TestPROOF03_GateNamesUnreadableFixture` (+ `intact_tree`, `defeated_tree_control`) | `proof03_test.go` | test | 03-03 |

**Modified — `internal/platform/gate`**

| Symbol | Kind | Change | Plan |
|---|---|---|---|
| `RunOptions.Dir` | exported struct field | new, additive | 03-01 |
| `DefaultTimeout` | exported const | value unchanged; rationale corrected and requirement-tagged | 03-01 |

**Governance artifacts**

| Path | Plan |
|---|---|
| `.planning/phases/03-automated-proof/03-GOV-SWEEP-RUNBOOK.md` | authored at planning time; invoked by path after the checkpoints |
| `.planning/phases/03-automated-proof/03-GOV-SWEEP.md` | written by the runbook; **no `*-SUMMARY.md`** (D-20) |

## Probe-row accounting (spec-less fallback)

The deterministic edge probe surfaced 10 rows across the phase's four requirement
IDs. This plan authors the **2 rows belonging to PROOF-02** (`empty`, `encoding`),
both **resolved explicit** as plain `must_haves.truths` strings. 03-01 authors
PROOF-01's 2 and GATE-10's 4; 03-03 authors PROOF-03's 2. **0 unresolved, 0
dismissed, 0 dropped** in this plan's section.

<verification>
- The gate fails and names `127.0.0.1:1` on one line when the database is set but unreachable (PROOF-02, GATE-10).
- The host asserted on is the value the test supplied, so no `testenv`, pgx or golang-migrate reword can break it for a reason nobody chose (D-15, D-17).
- The condition was reproduced without stopping the compose service, confirmed by inspecting the service after the run.
- D-03's ordering claim rests on two independent legs: the dry-run expansion and PROOF-02's in-run preamble observation (D-02).
- The ordering check is demonstrated to fail when the two recipe steps are transposed in a copy, and that demonstration is recorded rather than committed.
- The defeated copy shows no escalation preamble where the intact tree showed one, and the exit code is not a conjunct (D-11, D-14).
- The place where a reporter-identity assertion would go carries the reason it is absent (D-15).
- `make gate` is still green with the compose database up, and `git status --porcelain` is empty.
</verification>

<success_criteria>
- ROADMAP §Phase 3 success criterion 2 has an automated proof, and criterion 5 has its second of three differential controls.
- ROADMAP §Phase 2's carried D-03 ordering claim gains a structural check that survives a trim of the expensive proofs.
- `go build ./...`, `go vet ./...` and `gofmt -l .` are clean.
- Exactly two new files, both under `internal/platform/gateproof/`. No file created by 03-01 is modified.
- `go.mod` and `go.sum` are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-automated-proof/03-02-SUMMARY.md` when done.
</output>
