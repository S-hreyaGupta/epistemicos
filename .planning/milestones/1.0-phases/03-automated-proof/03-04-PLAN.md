---
phase: 03-automated-proof
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md
autonomous: true
requirements: [GATE-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`make planning-parity PHASE=3` exits 0 and reports that ROADMAP.md's Phase 3 requirement line and REQUIREMENTS.md's traceability table name the same four IDs — verified at the end of the phase as well as at its start, because the two documents were edited during it (GOV-01 obligation 1, D-13)"
    - "GATE-10's text in REQUIREMENTS.md makes clear it STRENGTHENS GATE-06 rather than duplicating it: GATE-06 remains the authority on WHAT an escalated failure says, GATE-10 on HOW IT ARRIVES, and a reader meeting GATE-10 alone takes away one requirement and its delivery guarantee, not two competing statements about the same message (D-19)"
    - "GATE-06 is byte-untouched by this phase and still reads `[x]` complete for Phase 2 — the historical record is written beside, never edited, and REQUIREMENTS.md's stated one-requirement-one-phase invariant holds even though `scripts/planning-parity.sh` does not enforce it (D-19)"
    - "GATE-10 is recorded as a declared amendment and NOT as a GOV-01 sweep correction, and the record states why: GATE-06's text is not false, it is incomplete, and adding a constraint is a scope act however small the change and however clearly the measurement shows no code moves (D-19)"
    - "ROADMAP §Phase 3's SC-5 correction names `buildChildEnv`'s deliberate `DepthEnv`-last ordering as WHAT FALSIFIED the original wording — not merely that it is false — because a criterion falsified by a later deliberate decision reads differently from one that was always wrong, and the next reader needs to know which (D-12)"
    - "The reason SC-5's original wording was unreachable is preserved rather than erased: `buildChildEnv` sets `DepthEnv` last and skips any entry naming it, so a control spawning the proof package hands it depth 1 and every proof `SkipIfNested`-skips — three skips and a green run, a vacuous pass inside the mechanism built to remove vacuous passes. The correction must therefore not read as a weakening (D-10, deferred item 4)"
    - "The SC-5 correction is recorded explicitly as GOV-01's SWEEP half — the requirement set is untouched and what changed is what a document says about it — and as that distinction's first use on a success criterion rather than on a status field (D-12)"
    - "`13071ff`'s recorded diagnosis is corrected with the ORIGINAL CLAIM RESTATED, the operating mechanism named (`isPhaseComplete` = `verification.status === 'passed'`, plus the staleness rule that a `*-VERIFICATION.md` is stale when a summary is newer than it), and WHAT THE WRONG DIAGNOSIS WOULD HAVE CAUSED recorded — teaching SUMMARY-to-PLAN pairing to recognise runbooks would have left `completed_phases` reading 1 (D-21)"
    - "That correction has a durable home OUTSIDE `.planning/STATE.md`. STATE.md carries a registered expiry — it has many writers and no owner, it was stale at `2d52a1e` and again after the Phase 2 sweep's own correction — so a correction living only there is one executor run from disappearing, which is the same class of loss D-21 exists to close (02-FINDING-01, D-21)"
    - "The shared-database coupling and the GOV-01 derivation amendment are REGISTERED as named deferred items carrying their measurements, not merely declared inside a plan — because a declaration inside a plan closes with the phase and a named deferred item does not (D-06, deferred items 1 and 2)"
    - "Phase 3's close sweep writes `03-GOV-SWEEP.md` and NO `*-SUMMARY.md`, its runbook filename does not end `-PLAN.md`, and the divergence from Phase 2 — whose sweep did write a SUMMARY — is stated explicitly rather than left for a reader to infer as an inconsistency (D-20)"
    - "No governance act in this phase is made silently: each of the three — the SC-5 correction, the GATE-10 amendment, and the `13071ff` diagnosis correction — is on the record with its original claim restated and its falsifier named"
  artifacts:
    - path: ".planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md"
      provides: "The durable, single-writer home for D-21's correction: the original claim restated, the measured operating mechanism, what the wrong diagnosis would have caused, and the citation to 13071ff"
      contains: "13071ff"
    - path: ".planning/STATE.md"
      provides: "The deferred-item registrations carrying their measurements, and a pointer from the in-place correction to its durable home"
      contains: "03-FINDING-01-phase-completion-diagnosis.md"
  key_links:
    - from: ".planning/ROADMAP.md §Phase 3 Requirements line"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "`make planning-parity PHASE=3`, which compares the two sets in both directions"
      pattern: "PROOF-01|PROOF-02|PROOF-03|GATE-10"
    - from: ".planning/REQUIREMENTS.md GATE-10"
      to: ".planning/REQUIREMENTS.md GATE-06"
      via: "GATE-10's own text, which cites GATE-06 as what it strengthens and divides authority — GATE-06 on what is said, GATE-10 on how it arrives"
      pattern: "GATE-06"
    - from: ".planning/STATE.md's 2026-09-03 correction entry"
      to: ".planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md"
      via: "an explicit pointer, so the correction survives STATE.md's next executor rewrite"
      pattern: "03-FINDING-01"
    - "The SC-5 correction and the GATE-10 amendment look like the same kind of act and are deliberately not. One corrects what a document says about an untouched requirement set — a sweep. The other changes the set — a decision. GOV-01's own text turns on telling them apart, and this phase is the first place the distinction is applied to a success criterion. If a later reader merges them, the precedent becomes licence to strengthen requirements under a sweep."
    - "D-21's correction is the second instance this week of a registered finding naming a mechanism that was not the one operating. If its durable home is not established, the third instance will be diagnosed against the same wrong mechanism, because STATE.md is where anyone would look and STATE.md is the file with the registered expiry."
  prohibitions:
    - "No requirement may be added, strengthened or reworded under the guise of a sweep. Correcting what a document says about the requirement set is a sweep; changing the set is a decision that halts for a human. This plan performs one of each and must keep them labelled as such, because the distinction only has value if it is held when holding it is inconvenient."
    - "No historical record may be edited to make the present tidier. GATE-06 stays byte-untouched and accurate for what it required and when; `13071ff`'s commit message is immutable and is cited rather than pretended away; the correction is written beside the original claim with that claim restated, never in place of it."
    - "No deferred item may be recorded without the measurement that sized it. An item registered as a description rather than as a measurement is one a later reader cannot act on and will re-derive from scratch or drop — which is the failure that made these items worth registering rather than declaring."
    - "No confirmation may be written for a document that was not actually re-read against this phase's final requirement set and decisions. A verdict that says checked without being checked is the precise failure GOV-01 exists to remove, reproduced inside GOV-01's own mechanism."
---

<objective>
Close the governance half of Phase 3: confirm that the two governance acts already
landed carry everything CONTEXT.md requires them to carry, give D-21's correction a
home that survives STATE.md's next rewrite, and register the two deferred items with
their measurements so they travel past this phase.

Purpose: this phase performed two governance acts of deliberately different kinds —
a **sweep correction** (ROADMAP SC-5, which corrects what a document says about an
untouched requirement set) and a **scope amendment** (GATE-10, which changes the set).
Both landed at `3fd4df5` before any planner was spawned, which is what GOV-01
obligation 1 exists to force. What has not been done is *verifying* they carry their
required content, and what has not been closed is D-21's correction, which currently
lives only in the file with a registered expiry.

Output: a verified requirement set, a durable finding document for D-21, and two
registered deferred items.

## What is already done, and must be verified rather than redone

The orchestrator landed these before spawning the planner. This plan **checks** them
and corrects only a gap it finds; it does not re-perform them.

| Act | Where | Commit | This plan's job |
|---|---|---|---|
| GATE-10 declared in REQUIREMENTS.md and added to ROADMAP §Phase 3's Requirements line | `REQUIREMENTS.md`, `ROADMAP.md` | `3fd4df5` | verify content against D-19's five stated obligations |
| ROADMAP §Phase 3 SC-5 corrected, with a correction block beneath it | `ROADMAP.md` | `3fd4df5` | verify content against D-12's two stated obligations |
| `make planning-parity PHASE=3` green | — | — | re-run at phase end, because both documents were edited during the phase |
| `13071ff`'s diagnosis corrected in place in STATE.md | `.planning/STATE.md` | `02881b4` | verify against D-21's three elements, then give it a durable home |
| D-07's "established pattern" claim corrected in CONTEXT.md | `03-CONTEXT.md` | `1ae320f` | no action — consumed by 03-01's discretion note 1 |

## Assumption-delta decision (second instance)

03-01 recorded the package-identity instance. This plan records the requirement one,
because it is the one the detector's `pluralization` family actually points at.

- **Noun now primary:** *the requirement governing the escalated failure message*.
  Phase 2 had exactly one (GATE-06); this phase introduces a second (GATE-10).
- **Decision: `add-alongside`, deliberately.** D-19 rejected promoting a single merged
  "escalated failure message contract" requirement, and rejected re-listing GATE-06
  under Phase 3. Promotion would have edited a frozen historical record and given
  GATE-06 an ambiguous status — `[x]` complete for Phase 2 and pending for Phase 3 —
  while frozen Phase 2 artifacts already cite its completion. It would also have
  broken REQUIREMENTS.md's stated one-requirement-one-phase invariant, which
  `scripts/planning-parity.sh` does *not* enforce, so the breakage would have been
  silent.
- **Accepted debt:** two requirements now speak about the same message, and a sweep
  that finds both must be able to tell which is authoritative. GATE-10's text carries
  the division explicitly — GATE-06 on *what* is said, GATE-10 on *how it arrives* —
  and Task 1 verifies that division is actually present rather than assumed.
- **What would force a promote:** a third constraint on the same message. At that
  point the division stops being two-sided and a merged message-contract requirement
  becomes the honest primary.
- **Invariant test:** already delivered rather than suggested. The three proofs'
  same-line assertions ARE the invariant test for the generalized intent — every
  escalated path delivers preamble and cause in one message, for every supported
  condition. They go red the instant a future phase splits a `Fatalf`, which is
  exactly the regression GATE-10 exists to make visible.
</objective>

<execution_context>
@C:/EpistemicOS/epistemicos-gsd-pilot/.claude/gsd-core/workflows/execute-plan.md
@C:/EpistemicOS/epistemicos-gsd-pilot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-automated-proof/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the two governance acts carry what CONTEXT.md requires, and re-run the parity check</name>

  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` — GATE-10's full entry (its five paragraphs), GATE-06's entry (which must be byte-untouched), the Traceability table's Phase 3 rows, and the 2026-09-03 correction block near the table that separates the sweep half from the scope half
    - `.planning/ROADMAP.md` §Phase 3 in full — the `**Requirements**:` line, the `**Requirements note**:`, all six success criteria, and the "Correction to criterion 5, 2026-09-03" block beneath them
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` — **D-12** in full (its two numbered obligations for the correction) and **D-19** in full (its five stated obligations for the amendment)
    - `scripts/planning-parity.sh` — what the check actually compares, its ID regex `[A-Z]{2,}-[0-9]{2}`, and the explicit fact that it does NOT enforce one-requirement-one-phase
    - `git show 3fd4df5 --stat` and the diff — what actually landed, so the verification is against the bytes rather than against the intention
  </read_first>

  <behavior>
    - `make planning-parity PHASE=3` exits 0 and reports four requirement IDs compared and agreeing.
    - GATE-06's entry in REQUIREMENTS.md is byte-identical to its state at the phase base — `git diff 7a80ff6 -- .planning/REQUIREMENTS.md` shows no change inside GATE-06's block.
    - GATE-10's entry states the strengthens-not-duplicates relationship, names GATE-06, divides authority between them, and records why it is a declared amendment rather than a sweep correction.
    - ROADMAP SC-5's correction block names `buildChildEnv`'s `DepthEnv`-last ordering as the falsifier and labels itself GOV-01's sweep half.
  </behavior>

  <action>
Verify, do not redo. Both edits landed at `3fd4df5`; this task confirms their content
against the obligations CONTEXT.md attaches, and corrects **only** a gap it actually
finds — recording any correction on the record with the original text restated.

**Check GATE-10's entry in REQUIREMENTS.md against D-19's five obligations:**

1. It states that it **strengthens** GATE-06 and does not duplicate it, and divides
   authority explicitly: GATE-06 is the authority on *what* an escalated failure says;
   GATE-10 on *how it arrives*. A reader meeting GATE-10 alone must take away one
   requirement and its delivery guarantee, not two competing statements about the same
   message — otherwise the next sweep finds two requirements about one message and
   cannot tell which is authoritative.
2. It records *why it became a requirement*: Phase 3's proofs assert same-line
   delivery because the weaker forms are measurably vacuous, and without GATE-10
   someone splitting a `t.Fatalf` in two would break three proofs **without violating
   any written requirement**, so the failure would read as a proof defect rather than
   as the requirement change it is.
3. It records that **no code change is entailed** — all three escalated paths already
   satisfy it, measured at declaration time — and that it is nonetheless declared as
   scope rather than filed as a sweep correction, because the requirement set is what
   changes.
4. It is a **new ID**, not a re-listed GATE-06, and states the three reasons: GATE-06
   stays byte-untouched and accurate for what it required and when; re-listing would
   have broken the file's own one-requirement-one-phase invariant, which the parity
   script does not enforce so the breakage would have been silent; and re-listing would
   have given GATE-06 an ambiguous status while frozen Phase 2 artifacts cite its
   completion.
5. GATE-06's own entry is **byte-untouched**.

**Check ROADMAP §Phase 3's SC-5 correction against D-12's two obligations:**

1. It names `buildChildEnv`'s deliberate `DepthEnv`-last ordering as **what
   falsified** the original wording — not merely that the wording is false. A
   criterion falsified by a later deliberate decision reads differently from one that
   was always wrong, and the next reader needs to know which. Same shape as
   `harness.go`'s timeout rationale and `ci.yml`'s job name.
2. It records itself explicitly as GOV-01's **sweep half** — the requirement set is
   untouched; this corrects what a document *says* about it — and as that
   distinction's **first use on a success criterion rather than on a status field**.

Additionally confirm that the correction preserves **why** the original wording could not
stand (D-10), not merely that it is corrected: `buildChildEnv` sets `DepthEnv` last and
skips any entry naming it, so a control that spawns the proof package hands it depth 1,
every proof `SkipIfNested`-skips, and the control observes three skips and a green run —
a vacuous pass inside the mechanism built to remove vacuous passes. Deferred item 4
carries this so the correction is not later mistaken for a weakening; if the ROADMAP
block does not carry it, add it and record the addition.

Also confirm ROADMAP §Phase 3's `**Requirements**:` line reads
`PROOF-01, PROOF-02, PROOF-03, GATE-10` and that its `**Requirements note**` records
that GATE-10 was declared during this phase's discussion rather than at a phase-close
checkpoint, and that recording it before any planner was spawned is what GOV-01
obligation 1 exists to force.

Then update ROADMAP §Phase 3's `**Plans**:` line and plan list to the actual plan set
this phase executed, replacing the `TBD` placeholders, and mark each plan's checkbox
according to its SUMMARY. Note the close sweep separately and explicitly as a
post-checkpoint runbook that is not a wave plan.

Finally re-run `make planning-parity PHASE=3`. It must exit 0. Record its exact output
line in the SUMMARY as evidence, not a paraphrase. If it fails, **HALT** — do not
repair one document to match the other.
  </action>

  <verify>
    <automated>make planning-parity PHASE=3</automated>
    <automated>git diff 7a80ff6 -- .planning/REQUIREMENTS.md | grep -c 'GATE-06' </automated>
  </verify>

  <acceptance_criteria>
    - `make planning-parity PHASE=3` exits 0 and its output names 4 requirement IDs compared and agreeing; the exact line is quoted in the SUMMARY.
    - `grep -c 'GATE-10' .planning/ROADMAP.md` is at least 1 and `grep -n 'PROOF-01, PROOF-02, PROOF-03, GATE-10' .planning/ROADMAP.md` matches the Phase 3 Requirements line.
    - GATE-10's entry in REQUIREMENTS.md contains all five of: the token `GATE-06`, the word `STRENGTHENS` (or an unambiguous equivalent stating the relationship), a statement that no code change is entailed, a statement that it is a NEW ID rather than a re-listing, and the one-requirement-one-phase reasoning. Each is confirmed by reading, and any missing one is added with the addition recorded.
    - `git diff 7a80ff6 -- .planning/REQUIREMENTS.md` shows no changed line inside GATE-06's entry — the historical record is untouched.
    - ROADMAP's SC-5 correction block contains the token `buildChildEnv` and the words identifying it as GOV-01's sweep half.
    - ROADMAP §Phase 3's `**Plans**:` line and plan list no longer contain `TBD`, and list `03-01`, `03-02`, `03-03`, `03-04` with their wave grouping plus the close-sweep runbook noted separately as not a wave plan.
    - Any correction this task makes is recorded with the original text restated, never applied silently.
  </acceptance_criteria>

  <done>
The requirement set is verified in both documents and the parity check is green at
phase end as well as at phase start. GATE-10 reads as a strengthening of GATE-06 with
authority divided, GATE-06 is byte-untouched, and SC-5's correction names its falsifier
and labels itself a sweep. ROADMAP's plan list reflects what this phase actually built.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give D-21's correction a durable home outside STATE.md</name>

  <files>.planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` — the `### Blockers/Concerns — added 2026-09-03` section in full: the correction the orchestrator already landed at `02881b4`, which restates `13071ff`'s claim, names the operating mechanism, and records what the wrong diagnosis would have caused
    - `.planning/STATE.md` — the "This file's accuracy has a known expiry" paragraph under Current Position, and the FINDING-01 entries under `### Blockers/Concerns — added 2026-09-02`
    - `.planning/phases/02-enforcement-and-a-single-gate-definition/02-FINDING-01-state-authority.md` — the shape this new document follows, and the precedent for an in-place correction with the original claim restated
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` **D-21** in full, and **deferred item 5**, which carries D-21's scope so it travels if the phase is rescoped
    - `git log -1 13071ff --format=%B` — the immutable commit message carrying the wrong diagnosis, cited rather than pretended away
  </read_first>

  <behavior>
    - The new finding document restates `13071ff`'s original claim verbatim in substance before correcting it, so the correction is legible without reading the commit.
    - It names the operating mechanism with its file and line: `isPhaseComplete` at `.claude/gsd-core/bin/lib/verification.cjs:565`, which is `verification.status === 'passed'`, plus the staleness rule that a `*-VERIFICATION.md` is stale when a summary is newer than it.
    - It records what the wrong diagnosis would have caused: teaching SUMMARY-to-PLAN pairing to recognise runbooks would have left `completed_phases` reading 1, because pairing is not what the counter consults.
    - It records that this is the second instance this week of a registered finding naming a mechanism that was not the one operating, and names FINDING-01's normalizer diagnosis as the first.
    - STATE.md's existing 2026-09-03 entry gains a pointer to the new document and is otherwise unchanged.
  </behavior>

  <action>
First **audit** the correction the orchestrator landed at `02881b4` in STATE.md's
`### Blockers/Concerns — added 2026-09-03` section against D-21's three required
elements: the original claim restated, the operating mechanism named and measured, and
what the wrong diagnosis would have caused. Report the audit verdict per element in
the SUMMARY. Correct in place only an element that is genuinely absent, restating what
was there.

Then create
`.planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md`,
following `02-FINDING-01-state-authority.md`'s shape.

**Why a new document rather than leaving the correction where it is.** This is the
substantive judgement in this task and it belongs in the document's own opening. The
correction currently lives only in `.planning/STATE.md`, which carries a *registered
expiry*: FINDING-01 records that the file has many writers and no owner, that it was
hand-corrected at `20bd4ce` and then rewritten by three executors, that it was stale
again by `2d52a1e`, and that it was stale a further time after the Phase 2 sweep's own
correction. A correction that lives only there is one executor run from disappearing —
which is the same class of loss D-21 exists to close, reproduced inside D-21's own fix.
The new document has one writer, is tracked under `.planning/`, names phase 3 and is
reachable by the GOV-01 close sweep's derivation. Its filename does not end `-PLAN.md`,
so `plan-scan.cjs` does not schedule it.

The document carries, in this order:

1. **The original claim, restated** — `13071ff`'s commit message and the finding
   registered from it both named SUMMARY-to-PLAN pairing: that the phase-completion
   heuristic does not recognise the post-checkpoint runbook's SUMMARY
   (`02-05-SUMMARY.md`, associated with `02-GOV-SWEEP-RUNBOOK.md` rather than a
   `*-PLAN.md`) as completing Phase 2. Quote it, so the correction is legible without
   reading the commit.
2. **Why it is wrong, and inverted** — the pairing never enters the computation.
   `scanPhasePlans`'s SUMMARY/PLAN counts feed `total_plans` and `completed_plans`,
   different fields entirely. Phase completion routes through `isPhaseComplete`
   (`.claude/gsd-core/bin/lib/verification.cjs:565`), which is
   `verification.status === 'passed'`, and the staleness rule is that a
   `*-VERIFICATION.md` is stale when a summary is newer than it. `02-05-SUMMARY.md` is
   not *unrecognised* — it **is** seen, it **is** newer than `02-VERIFICATION.md`, and
   being seen is exactly what marks verification stale. Record the live measurement:
   phase 01 read `passed`, phase 02 read `stale`.
3. **What the wrong diagnosis would have caused** — the part D-21 says is worth
   keeping. Teaching the SUMMARY/PLAN pairing to recognise runbooks, the fix the
   recorded mechanism implies, would have changed nothing: `completed_phases` would
   still read 1, because pairing is not what the counter consults. A wrong mechanism
   yields a wrong fix that *appears* responsive.
4. **The class, named** — second instance this week of a registered finding describing
   a mechanism that was not the one operating, FINDING-01's normalizer diagnosis being
   the first. Both were explanations that fit the evidence, asserted rather than
   measured, and both were falsified by someone reading the code path instead of the
   finding.
5. **The structural consequence and Phase 3's answer** — GOV-01 requires the close
   sweep to run *after* verification and to commit, so any phase whose sweep writes a
   SUMMARY ends with stale verification and an undercounted phase. D-20 is the answer:
   Phase 3's sweep writes `03-GOV-SWEEP.md` and no `*-SUMMARY.md`, so the staleness
   comparison has nothing newer to find. Record the verified fact that the SUMMARY
   filename is **not** mandated by GOV-01 — its text never mentions a summary — and
   that the "own commit and summary" wording is Phase 2's own D-16, a CONTEXT decision.
   So this is a plan-shape choice, not a requirement change, and needs no GATE-10-style
   declaration.
6. **Citation** — `13071ff` is immutable, so this document is the correction and the
   commit is the citation. Do not attempt to alter the commit message.

Finally, add a single pointer line to STATE.md's existing 2026-09-03 entry naming this
document as the correction's durable home and stating the expiry reason. Change nothing
else in that entry — the entry is the in-place correction D-21 asked for and it stays.
  </action>

  <verify>
    <automated>test -f .planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md &amp;&amp; grep -c '13071ff' .planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md</automated>
    <automated>grep -c '03-FINDING-01-phase-completion-diagnosis' .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md` exists, is tracked by git, and its filename does not end `-PLAN.md`.
    - It contains all six of: the token `13071ff`; the restated original claim naming SUMMARY-to-PLAN pairing; the token `isPhaseComplete`; the phrase recording that the wrong fix would have left the counter reading 1; the token `FINDING-01` naming the first instance of the class; and the token `D-20` connecting to Phase 3's answer.
    - `grep -c 'verification.cjs' .planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md` is at least 1 — the mechanism is cited by file, not described.
    - `.planning/STATE.md` contains exactly one new pointer line naming the finding document, and `git diff -- .planning/STATE.md` shows no other change inside the `### Blockers/Concerns — added 2026-09-03` section.
    - The SUMMARY records a per-element audit verdict for D-21's three required elements against STATE.md's existing correction — present, or corrected with the original restated. No element is reported as checked without having been read.
    - `git log -1 --format=%H -- .planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md` returns a commit — the document is committed, not left untracked.
  </acceptance_criteria>

  <done>
D-21's correction has a single-writer, tracked, sweep-reachable home that survives
STATE.md's next executor rewrite. It restates the original claim, names the measured
mechanism by file and line, records what the wrong fix would have failed to do, names
the class and its first instance, and connects to D-20 as the structural answer.
STATE.md's in-place correction stays and points at it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register the deferred items with their measurements, so they travel</name>

  <files>.planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` — the `## Deferred Items` table (its columns, and the two existing rows' level of detail, which is the bar this task meets), and the `### Pending Todos` section added by the Phase 2 close sweep
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` `<deferred>` — all five entries, and the opening sentence explaining why they are registered rather than absorbed
    - `.planning/phases/03-automated-proof/03-CONTEXT.md` **D-06** — the measured per-package intervals and the concurrency deltas
    - `.planning/REQUIREMENTS.md` GOV-01 obligations 3 and 4 — the derivation rule and its coverage bound, which deferred item 2 proposes amending and which this phase deliberately does not amend
  </read_first>

  <behavior>
    - Deferred item 1 is registered with its measurement: `gate` x `store` +2.11s concurrent and `gate` x `approved` +0.80s, derived from per-package intervals in `go test ./... -json` (`store` 2.42s to 6.57s, `approved` 2.32s to 5.26s, `gate` 4.46s to 25.70s).
    - Deferred item 2 is registered with its measurement: widening the derivation to `git ls-files` costs 5.3s over 227 tracked files, takes the derived set from 51 to 54, and adds one false positive.
    - Both rows state the reason for deferral, not just the fact of it.
    - Deferred item 2's row records why it was refused *for Phase 3 specifically* and when it is to be decided.
    - No requirement is added, and `make planning-parity PHASE=3` is still green afterwards.
  </behavior>

  <action>
Add two rows to `.planning/STATE.md`'s `## Deferred Items` table, matching the detail
level of the two rows already there. A row that describes rather than measures is a row
a later reader cannot act on — that is precisely why these were registered rather than
declared, so each carries its number.

**Row 1 — Test isolation: isolate PROOF-03's nested run on its own database or schema
(D-06, deferred item 1).**
Measured interference, present *today* before Phase 3: `gate` x `store` **+2.11s
concurrent**, `gate` x `approved` **+0.80s**, from per-package intervals in
`go test ./... -json` — `store` 2.42s to 6.57s, `approved` 2.32s to 5.26s, `gate` 4.46s
to 25.70s. Phase 2's nested `make test` already runs `store` tests against the compose
database while the outer `store` package is running; Phase 3 adds `migrate` to that set
via PROOF-03's full nested gate. **Not taken because it is arguably its own
requirement** — new machinery in a phase scoped to three proofs. Status: registered, not
planned. Deferred at: 2026-09-03, Phase 3 discussion. Milestone: Gate milestone.

**Row 2 — Governance: amend GOV-01's close-sweep derivation to IDs-only, repo-wide
(deferred item 2).**
The current rule is path-scoped — `git ls-files '.planning/*'` — so a requirement-ID tag
in a file outside `.planning/` buys nothing; `ci.yml` and `harness.go` would each stay
invisible **fully tagged**. Measured: widening to `git ls-files` costs **5.3s over 227
tracked files** and takes the derived set **51 to 54**, adding `harness.go` and
`testenv_test.go` correctly and `classify_test.go` as a **false positive**, matching on
"phase 3's done-condition" — the *segmentation pipeline's* phase 3. So the amendment must
be **IDs-only outside `.planning/`, with no phase-number term**: requirement IDs are
globally unambiguous, phase numbers are not, and the phase-number term is the sole noise
source. **Refused for Phase 3 deliberately**: Phase 3's own close sweep would then run
against a rule the phase changed — a check validating its own amendment, the same
self-reference that disqualified VERIFICATION.md as GOV-01's home. **Decide at milestone
close.** Nothing is lost by waiting: `harness.go` is already in the derived set today via
its "Phase 3" prose. Record the argument for the amendment in the form D-06 preserved,
because it is better than the one first given: the tag's value is **not** getting
`harness.go` into the set — it is already in, unprompted — it is **keeping it in when
someone rewords the prose**. A durability property, not a coverage one.

Then add one entry to `### Pending Todos` recording deferred items 3, 4 and 5 as carried
rather than closed, each in one line with a pointer to `03-CONTEXT.md`'s `<deferred>`
section: (3) the class of measured claims in code files outside `.planning/` that go
stale and are invisible to the derivation, with its two instances — `ci.yml`'s job name
and `harness.go`'s timeout rationale — and the observation that **neither was found by a
sweep; both were found by someone running something**; (4) SC-5's literal wording being
unreachable through the harness by design, preserved so the correction is not later
mistaken for a weakening; and (5) D-21's scope, so it travels if this phase is rescoped.

Do not add a requirement. Registering a deferred item is not a scope act; if any entry
here would require one, **HALT** and report it as a decision for a human — that is
GOV-01's own rule and this plan is not exempt from it.

Re-run `make planning-parity PHASE=3` afterwards to confirm nothing in this task
disturbed the requirement set.
  </action>

  <verify>
    <automated>make planning-parity PHASE=3</automated>
    <automated>grep -c '2.11s' .planning/STATE.md; grep -c '51' .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/STATE.md`'s `## Deferred Items` table gains exactly two rows, and each contains at least one measured number: row 1 contains `2.11s` and `0.80s`; row 2 contains `5.3s`, `227` and the `51` to `54` derived-set figures.
    - Each new row states a reason for deferral distinct from "not done yet", and row 2 states why it was refused *for Phase 3 specifically* and when it is to be decided.
    - `### Pending Todos` gains one entry covering deferred items 3, 4 and 5, each with a pointer to `03-CONTEXT.md`.
    - `grep -c 'ci.yml' .planning/STATE.md` is at least 1 within the new pending-todo entry — the bound's demonstrated instance travels with the class.
    - `make planning-parity PHASE=3` still exits 0 — no requirement was added or removed.
    - `git diff -- .planning/REQUIREMENTS.md` prints nothing for this task — registering a deferred item is not a scope act.
  </acceptance_criteria>

  <done>
The shared-database coupling and the derivation amendment are registered as named
deferred items carrying the measurements that sized them, and the three remaining
deferred entries are carried in Pending Todos with pointers. A declaration inside a plan
closes with the phase; these do not. No requirement moved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning documents → planner and executor agents | ROADMAP.md and REQUIREMENTS.md are the inputs every future agent plans against; a disagreement between them silently drops scope |
| STATE.md → every agent that reads project position | A file with many writers and no owner, whose contents are treated as authoritative by agents that did not write them |
| Governance record → future readers | A correction recorded without its original claim is indistinguishable from a document that was always right |
| `make planning-parity` → the requirement set | A shell target that reads both documents and is the only mechanical check that they agree |

## STRIDE Threat Register

ASVS L1; `security_block_on: high`. Severity is impact x likelihood.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Repudiation | a governance act made silently | **high** | mitigate | The named hazard of this plan, and the one the milestone's own history demonstrates: ROADMAP listed four Phase 2 requirements while REQUIREMENTS.md carried ten, and a planner reading four would have dropped six including GOV-01 itself. Three controls: the parity check runs at phase start *and* at phase end because both documents were edited in between; every correction restates the original claim so it is legible as a correction; and the sweep/scope distinction is labelled per act, so a later reader cannot read a strengthened requirement as a sweep |
| T-03-09 | Tampering | editing a frozen historical record | **high** | mitigate | GATE-06 must stay byte-untouched and `13071ff`'s commit message is immutable and cited rather than altered. Enforced by an acceptance criterion diffing REQUIREMENTS.md against the phase base and confirming no changed line inside GATE-06's entry. The house rule this implements — do not edit the historical record, write beside it — is on its fourth application in this project |
| T-03-10 | Repudiation | a correction that does not survive its host file | medium | mitigate | STATE.md carries a registered expiry: many writers, no owner, stale at `2d52a1e` and again after the Phase 2 sweep's own correction. A correction living only there is one executor run from vanishing. Task 2 gives D-21's correction a single-writer, tracked, sweep-reachable home and leaves a pointer behind, so the loss path is closed rather than noted |
| T-03-11 | Tampering | a deferred item registered without its measurement | medium | mitigate | An item recorded as a description is one a later reader re-derives or drops. Each new row is required by acceptance criteria to contain specific measured numbers, so a row that lost its measurement fails the check rather than reading as complete |
| T-03-12 | Elevation of Privilege | a sweep silently adding a requirement | medium | mitigate | GOV-01's own rule: a pass that would ADD a requirement halts for a human rather than proceeding. Task 3 states the halt explicitly and an acceptance criterion asserts `git diff -- .planning/REQUIREMENTS.md` prints nothing for that task |
| T-03-SC | Tampering | dependency installation | low | accept | No code, no package-manager install, no dependency change. This plan modifies only files under `.planning/` |
</threat_model>

## Artifacts this phase produces

This plan's contributions are marked **(here)**. The code surface is repeated so an
executor of this plan alone can see what the governance record must describe.

**New package — `internal/platform/gateproof`** (test-only)

| Symbol | File | Plan |
|---|---|---|
| `unreachableDSN`, `fixturePathSuffix`, `lineContainsBoth`, `requireIntact` | `gateproof_test.go` | 03-01 |
| `TestLineContainsBoth_Control`, `TestGateproof_NoParallelMarker` | `gateproof_test.go` | 03-01 |
| `materializeTree`, `stripEscalationExport`, `TestMaterializeTree_Control` | `tree_test.go` | 03-01 |
| `TestPROOF01_GateNamesUnsetURL` (+ `intact_tree`, `defeated_tree_control`) | `proof01_test.go` | 03-01 |
| `TestPROOF02_GateNamesUnreachableHost` (+ `intact_tree`, `defeated_tree_control`) | `proof02_test.go` | 03-02 |
| `TestGateOrdersPreflightBeforeMigrate` | `ordering_test.go` | 03-02 |
| `breakFixture`, `TestPROOF03_GateNamesUnreadableFixture` (+ two subtests) | `proof03_test.go` | 03-03 |

**Modified — `internal/platform/gate`**

| Symbol | Change | Plan |
|---|---|---|
| `RunOptions.Dir` | new exported struct field, additive | 03-01 |
| `DefaultTimeout` | value unchanged; rationale corrected and requirement-tagged | 03-01 |

**Governance artifacts**

| Path | Plan |
|---|---|
| `.planning/phases/03-automated-proof/03-FINDING-01-phase-completion-diagnosis.md` | 03-04 **(here)** |
| `.planning/phases/03-automated-proof/03-GOV-SWEEP-RUNBOOK.md` | authored at planning time; invoked by path after verification, UAT and security sign-off |
| `.planning/phases/03-automated-proof/03-GOV-SWEEP.md` | written by the runbook; **no `*-SUMMARY.md` accompanies it** (D-20) |

## Probe-row accounting (spec-less fallback)

The deterministic edge probe surfaced 10 rows across the phase's four requirement IDs,
all authored into `must_haves.truths` by the three code plans — PROOF-01's 2 and
GATE-10's 4 in 03-01, PROOF-02's 2 in 03-02, PROOF-03's 2 in 03-03. **9 resolved
explicit, 1 resolved backstop, 0 unresolved, 0 dismissed, 0 dropped.** This plan
authors no probe rows of its own: it changes no behavior, so it has no input edges.
Its `must_haves.truths` are governance invariants, not probe-derived predicates.

<verification>
- `make planning-parity PHASE=3` is green at phase end as well as at phase start, and its exact output is quoted as evidence (GOV-01 obligation 1, D-13).
- GATE-10 reads as strengthening GATE-06 with authority divided, and GATE-06 is byte-untouched (D-19).
- SC-5's correction names its falsifier and labels itself GOV-01's sweep half, on its first use against a success criterion (D-12).
- The two governance acts stay labelled as different kinds — one sweep, one scope — because the distinction only has value if it is held when holding it is inconvenient (D-12, D-19).
- D-21's correction is audited element by element and given a durable home outside the file with a registered expiry.
- The two deferred items are registered with the measurements that sized them, and three more are carried in Pending Todos (D-06, deferred items 1 through 5).
- No requirement was added, strengthened or reworded by any task in this plan.
- ROADMAP §Phase 3's plan list reflects what this phase actually built, with the close-sweep runbook noted as not a wave plan (D-20).
</verification>

<success_criteria>
- The requirement set is verified in both documents, mechanically, at the end of a phase that edited both.
- Every governance act this phase performed is on the record with its original claim restated and its falsifier named — none was made silently.
- A future reader meeting GATE-06 and GATE-10 together can tell which is authoritative for what.
- D-21's correction outlives the next executor run that rewrites STATE.md.
- The two deferred items travel past this phase carrying their measurements.
- `make gate` is green and no file outside `.planning/` was modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-automated-proof/03-04-SUMMARY.md` when done.
</output>
